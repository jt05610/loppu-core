type Stream{
    name: String
    items: [StreamItem!]
}

type Query {
    "All streams available"
    streams: [Stream!]!
    "The details of a specific stream"
    stream(name: String!): Stream
}

type KeyedValue {
    key: FieldSet,
}
type StreamItem {
    stream: Stream

}